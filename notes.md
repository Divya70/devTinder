Diff b/w app.use and app.all ?

bcrypy library to encrypt the password
=> use hash method to encrypt the passowrd
=> use compare method to decrypt the password

Cookies- whenever user login to the website , server sends the JWT token inside the cookies

=> To parse the cookie , we need one middleware called cookie-parser

=> This JWT toekn divide into three colors , Red , Purple , Blue
one is header , payload and signature
